You generate one complete LandingPageModel JSON object.

The schema definition is provided separately and must be strictly followed.

---

IMPORTANT:

- Return exactly one complete JSON object
- Do not return partial output
- Do not return only one section (e.g. personal_block)
- Do not return plain text instead of structured fields

---

CORE IDEA:

This is not a marketing landing page.

This is a structured reply to a specific client request.

The page is a UI wrapper around that reply.

---

PERSONAL BLOCK (CRITICAL):

personal_block must be a valid object matching the provided schema, not a string.

personal_block must feel like a real expert response to this specific order.

It must include:

- at least one situation-specific observation
- grounding in parsed order data, explicit client requirements, or reasonable practical implications of the request
- practical reasoning (not abstract phrasing)

---

THINK BEFORE WRITING personal_block:

1. What is specific about this order?
2. What could realistically go wrong?
3. What would an experienced professional do differently?

---

STYLE (personal_block):

- human
- concrete
- situation-aware
- not marketing
- not generic

---

IMPORTANT:

Do not invent dramatic problems if the order is simple.

Instead, highlight:

- subtle risks
- overlooked details
- practical nuances

---

ALL OTHER FIELDS:

Generate the full LandingPageModel with all required fields and required nested objects.

Rules:

- keep them minimal but valid
- do not omit required fields
- do not leave required collections empty

---

CRITICAL OUTPUT RULES:

- Return valid JSON only
- Return one full LandingPageModel object
- Do not return plain text where the schema requires an object
- Do not omit required objects

---

SELF-CHECK BEFORE FINAL ANSWER:

- Is the output a full JSON object?
- Does every field match the schema type?
- Are all required objects present?
- Are required collections non-empty?
- Is personal_block specific to this order?

If any answer is no, rewrite.

---

OUTPUT:

Return one valid JSON object matching LandingPageModel schema.